parameters:

  header:
    ## Header information is used for logging. It is good practice to give your simulations explicit names
    title: Acoustic-Elastic interface simulation # name for your simulation
    # A detailed description for your simulation
    description: |
      Material systems : Acoustic domain (1) over Elastic domain (1)
      Interfaces : Acoustic-Elastic conforming interface (1)
      Sources : Force source (1) in acoustic domain
      Boundary conditions : Neumann BCs on all edges
      Debugging comment : This test exercises the acoustic-elastic coupling at a flat conforming interface

  simulation-setup:
    ## quadrature setup
    quadrature:
      quadrature-type: GLL4

    ## Solver setup
    solver:
      time-marching:
        type-of-simulation: forward
        time-scheme:
          type: Newmark
          dt: 4e-2
          nstep: 1000

    simulation-mode:
      forward:
        writer:
          seismogram:
            format: ascii
            directory: "."

  receivers:
    stations: "displacement_tests/Newmark/serial/dim3/AcousticElasticInterface/STATIONS"
    angle: 0.0
    seismogram-type:
      - displacement
    nstep_between_samples: 1

  ## Runtime setup
  run-setup:
    number-of-processors: 1
    number-of-runs: 1

  ## databases
  databases:
    mesh-database: "displacement_tests/Newmark/serial/dim3/AcousticElasticInterface/DATABASES_MPI/external_mesh.bin"
    mesh-parameters: "displacement_tests/Newmark/serial/dim3/AcousticElasticInterface/DATABASES_MPI/mesh_parameters.bin"

  sources: "displacement_tests/Newmark/serial/dim3/AcousticElasticInterface/force.yaml"
