id : "AcousticElasticForce"
name : "Acoustic Elastic Interface - Explosive Force"
description: >
  Testing newmark time-marching solver on a acoustic-elastic interface. Test is run on a single MPI process.
config:
  nproc : 1
  tolerance : 1e-2
databases:
  specfem_config: "specfem_config.yaml"
  traces: "traces"
