id: "TwoLayerElectromagneticDomain"
name: "Two-layer electromagnetic domain (P-SV waves)"
description: >
  Testing newmark time-marching solver on a two-layer electromagnetic domain. Test is run on a single MPI process.
config:
  nproc: 1
  tolerance: 1e-3
databases:
  specfem_config: "specfem_config.yaml"
  traces: "traces"
