id: "AcousticElasticNonconformingCoupledDomain"
name: "Acoustic-Elastic nonconforming coupled domain (Test 1/2)"
description: >
  Testing newmark time-marching solver on a coupled acoustic-elastic domain with 1 elastic-acoustic interface. The orientation of the interface is horizontal with acoustic domain on top.
  A nonconforming interface is used to couple the two domains.
  Test is run on a single MPI process.

config:
  nproc: 1
  tolerance: 1e-2
databases:
  specfem_config: "specfem_config.yaml"
  traces: "traces"
