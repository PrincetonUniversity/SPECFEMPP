[project]
name = "specfempp-scripts"
version = "0.1.0"
description = "Nested project to store SPECFEM++ scripts and modules."
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["uv_build==0.11.24"]
build-backend = "uv_build"

[tool.uv.build-backend]
namespace = true
module-root = ""
