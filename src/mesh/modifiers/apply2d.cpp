#include "mesh/modifiers/modifiers.hpp"

template<>
void specfem::mesh::modifiers::apply(specfem::mesh::mesh<specfem::dimension::type::dim2>& mesh) const{
  // ===== subdivisions =====
  //find nspec for each material

  //add control nodes

  //add elements
}