#pragma once

/**
 * @brief Umbrella header for coordinate projection conversions.
 *
 * Includes all available map projections (currently UTM). To add a new
 * projection, place its header in the `projections/` subdirectory and
 * add an `#include` here.
 */

#include "projections/utm.hpp"
