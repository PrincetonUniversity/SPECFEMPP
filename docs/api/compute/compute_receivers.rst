.. _compute_receivers::

Receivers Struct
================

The ``receivers`` struct is used to store receiver information, computed receiver seismograms, and receiver elemental properties (lagrange interpolants) within the mesh.

.. doxygenstruct:: specfem::compute::receivers
    :members:
    :undoc-members:
